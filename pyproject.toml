[build-system]
requires = ["setuptools>=68", "wheel"]
build-backend = "setuptools.build_meta"

[project]
name = "codeintel"
version = "0.1.0"
description = "Local code intelligence engine for legacy Python codebases"
requires-python = ">=3.10"